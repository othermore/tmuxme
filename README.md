# tmuxme
A simple init script for bash. Added to .bashrc or simply run at the beginning, it offers an interactive option to tmux the session or connect to existing sessions. After 10s, it ends without doing anything.
